import { defaultSectionStyle } from "../defaultStyles";

const {
  headerStyle,
  subHeaderStyle,
  subSubHeaderStyle,
  normalStyle,
  nameStyle,
} = defaultSectionStyle;

export {
  headerStyle,
  subHeaderStyle,
  subSubHeaderStyle,
  normalStyle,
  nameStyle,
};
